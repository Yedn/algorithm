//子集和数问题

#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main()
{
    int W,M;
    cin>>W>>M;
    vector<int> w(W);
    for(int i=0;i<W;i++)
        cin>>w[i];
    sort(w.begin(),w.end());
    
}